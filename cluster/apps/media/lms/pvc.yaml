---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lms-config-v1
  namespace: media
  labels:
    app.kubernetes.io/name: &name lms
    app.kubernetes.io/instance: *name
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: nfs-client
